GIT CLONE ‘lien vers vôtre github’
GIT ADD FILENAME

GIT COMMIT -m  ‘description’

GIT PUSH -u ORIGIN main

GIT pull or GIT pull --all (Fetch all remotes - this is handy if you are working on a fork or in another use case with multiple remotes.)

Git branch add-create-branch-command

Git checkout add-create-branch-command
